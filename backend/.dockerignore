# Environments & secrets
.env
.env.*
!.env.production.example

# Caches
.hf_cache
.pytest_cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual envs
venv/
.venv/

# Tests / scripts (not needed in runtime image)
tests/

# Runtime data (created at runtime, not build time)
data_runtime/

# Git
.git/
.gitignore

# Local YOLO weights (downloaded in Dockerfile instead)
*.pt

# Node.js — bridge node_modules rebuilt in Docker stage 2
whatsapp-bridge/node_modules/
whatsapp-bridge/.baileys_auth/

# IDE / Editor
.vscode/
.idea/
*.iml

# Docs
*.md
!README.md