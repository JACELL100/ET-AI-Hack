services:
  - type: web
    name: raksha-ai-backend
    env: docker
    plan: starter
    rootDir: backend
    dockerfilePath: Dockerfile
    autoDeploy: true
    healthCheckPath: /health
    disk:
      name: raksha-ai-runtime
      mountPath: /app/data_runtime
      sizeGB: 1